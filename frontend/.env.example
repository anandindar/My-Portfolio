# Rename this file to .env and update the values before running locally
# MONGODB_URI is required by the Express server. VITE_CONTACT_ENDPOINT defaults to
# /api/contact, but you can override it if you host the API elsewhere.
MONGODB_URI=mongodb+srv://anandkumargupta0814_db_user:<db_password>@my-portfolio.mpzqix8.mongodb.net/?appName=My-Portfolio
VITE_CONTACT_ENDPOINT=/api/contact
